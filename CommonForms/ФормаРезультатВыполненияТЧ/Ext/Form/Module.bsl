
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Перем РезультатВыполненияТЧ;
		Параметры.Свойство("РезультатВыполненияТЧ");

	ПараметрыЗаполнены = ЗначениеЗаполнено(Параметры.РезультатВыполненияТЧ);
	Если НЕ ПараметрыЗаполнены Тогда
		Возврат;
	КонецЕсли;

	РезультатВыполненияТЧ = Параметры.РезультатВыполненияТЧ;

КонецПроцедуры
