
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Перем ТекстРезультатВыполнения;
		Параметры.Свойство("ТекстРезультатВыполнения");

	ПараметрыЗаполнены = ЗначениеЗаполнено(Параметры.ТекстРезультатВыполнения);
	Если НЕ ПараметрыЗаполнены Тогда
		Возврат;
	КонецЕсли;

	РезультатВыполнения = Параметры.ТекстРезультатВыполнения;

КонецПроцедуры
